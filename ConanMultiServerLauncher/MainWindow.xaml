<Window x:Class="ConanMultiServerLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Conan Multi-Server Launcher" Background="#2B2B2B" Foreground="#E0E0E0" Icon="Resources/App.ico"
        SizeToContent="Width" ResizeMode="CanResize" Loaded="MainWindow_Loaded">
    <Window.Resources>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#1F1F1F"/>
        <SolidColorBrush x:Key="InputBorderBrush" Color="#555555"/>
        <SolidColorBrush x:Key="InputForegroundBrush" Color="#E0E0E0"/>
        <!-- Override system brushes used by default ComboBox template to ensure dark dropdown -->
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#262626"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#E0E0E0"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource InputForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource InputForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource InputForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource InputForegroundBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#2A2A2A"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3A3A3A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#262626"/>
            <Setter Property="Foreground" Value="{StaticResource InputForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource InputForegroundBrush}"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource InputForegroundBrush}"/>
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Scroll visuals aligned to dark theme -->
        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="#262626"/>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#262626"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
        </Style>
        <!-- Ensure CheckBox text (e.g., Enable BattleEye) is light grey in dark theme -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource InputForegroundBrush}"/>
        </Style>
        
        <!-- Dark theme tooltip styling -->
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#2B2B2B"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Profile selection / creation -->
        <StackPanel Orientation="Horizontal">
            <ComboBox x:Name="ProfilesCombo" Width="240" DisplayMemberPath="Name" SelectionChanged="ProfilesCombo_SelectionChanged">
                <ComboBox.Template>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="2"/>
                            <ContentPresenter Margin="6,2,24,2"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                            <ToggleButton x:Name="DropDownToggle"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Stretch"
                                          Width="24"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False"
                                          ClickMode="Press">
                                <Path HorizontalAlignment="Center" VerticalAlignment="Center" Fill="{TemplateBinding Foreground}" Data="M 0 0 L 4 4 L 8 0 Z"/>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="#262626" BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="1">
                                    <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </ComboBox.Template>
            </ComboBox>
            <Button Content="New Profile" Width="120" Margin="8,0,0,0" Click="NewProfile_Click"/>
            <Button Content="Save Profile" Width="120" Margin="8,0,0,0" Click="SaveProfile_Click"/>
            <Button Content="Delete Profile" Width="120" Margin="8,0,0,0" Click="DeleteProfile_Click"/>
            <Button Content="Open Profiles Folder" Width="170" Margin="8,0,0,0" Click="OpenProfilesFolder_Click"/>
            <Button Content="Settings" Width="110" Margin="8,0,0,0" Click="OpenSettings_Click"/>
        </StackPanel>

        <!-- Profile details -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
            <StackPanel Width="300">
                <TextBlock Text="Profile Name"/>
                <TextBox x:Name="ProfileName"/>
                <CheckBox x:Name="BattlEyeCheckbox" Content="Enable BattleEye" Margin="0,8,0,0"/>
            </StackPanel>
            <StackPanel Width="300" Margin="16,0,0,0">
                <TextBlock Text="Server Address (ip:port)"/>
                <TextBox x:Name="ServerAddress"/>
            </StackPanel>
            <StackPanel Width="300" Margin="16,0,0,0">
                <TextBlock Text="Server Password (optional)"/>
                <PasswordBox x:Name="ServerPassword"/>
            </StackPanel>
        </StackPanel>

        <!-- Mods parser -->
        <GroupBox Grid.Row="2" Header="Mods">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Paste Mods" Width="120" Click="PasteMods_Click"/>
                        <Button Content="Paste Collection" Width="140" Margin="8,0,0,0" Click="PasteCollection_Click"/>
                        <Button Content="Load from .txt" Width="140" Margin="8,0,0,0" Click="LoadModsFromFile_Click"/>
                        <Button Content="Clear Mods" Width="120" Margin="8,0,0,0" Click="ClearMods_Click"/>
                        <TextBlock x:Name="ModsCountText" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                    <Grid Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Row 0: ConanSandbox servermodlist.txt path and actions -->
                        <Button Grid.Row="0" Grid.Column="0" Content="Locate servermodlist.txt" Width="180" Click="LocateModList_Click"/>
                        <Button Grid.Row="0" Grid.Column="1" Content="Locate Workshop 440900" Width="200" Margin="8,0,0,0" Click="LocateWorkshop_Click"/>
                        <TextBox Grid.Row="0" Grid.Column="2" x:Name="CurrentModListPath" Margin="8,0,0,0" VerticalAlignment="Center" IsReadOnly="True" BorderThickness="0" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" Visibility="Collapsed"/>

                        <!-- Row 1: LocalAppData modlist.txt path (read-only info) -->
                        <TextBox Grid.Row="1" Grid.Column="2" x:Name="CurrentLocalModListPath" Margin="8,6,0,0" VerticalAlignment="Center" IsReadOnly="True" BorderThickness="0" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" Visibility="Collapsed"/>
                    </Grid>
                </StackPanel>

                <ListBox x:Name="ModsList" Grid.Row="1"/>

                <Button Grid.Row="2" HorizontalAlignment="Right" Width="220" Content="Write mod lists" ToolTip="Writes ConanSandbox\\servermodlist.txt and LocalAppData\\modlist.txt" Click="WriteModList_Click"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Launch Conan" Height="40" Width="200" Click="Launch_Click"/>
            <Button Content="Kill Conan Tasks" Height="40" Width="200" Margin="8,0,0,0" Click="KillConan_Click"
                    ToolTip="Sometimes Conan does not complete its tasks, especially with Battle Eye. This button closes the tasks."
                    ToolTipService.InitialShowDelay="0"
                    ToolTipService.ShowDuration="30000"
                    ToolTipService.BetweenShowDelay="0"
                    ToolTipService.Placement="Top"/>
        </StackPanel>
    </Grid>
</Window>
